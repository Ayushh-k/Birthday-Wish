# Birthday Wish 🎂

A beautiful, animated static website to wish someone a very Happy Birthday!

## 🚀 How to Run Locally

Since this is a static site, you have two options:

**Option 1: Node.js (Recommended)**
1. Run `npm install` to install the local server.
2. Run `npm run server-node` to start the server.
3. Open `http://localhost:8081` in your browser.

**Option 2: Direct File Open**
Simply double-click on `index.html` in your file explorer to open it in your default web browser.

## 🎨 Customizing

* **Images**: Replace the photos inside the `assets/images` directory (`photo1.jpg`, `photo2.jpg`, etc.) with your own. Ensure `person.png` has a transparent background for best results on the cake.
* **Text**: Open `index.html` in your code editor and change the greetings inside the balloons and message paragraphs.
* **Audio**: Replace `assets/audio/hbd.mp3` with any music track you like.

## ✨ Technologies Used
* HTML5 / CSS3
* LESS (for cake animations)
* jQuery & Bootstrap
